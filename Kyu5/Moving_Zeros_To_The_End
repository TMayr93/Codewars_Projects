#move zeros in an array to the end

def move_zeros(array):
    res = []
    count = 0
    for i in array:
        if i == 0 and i is not False:
            count += 1
        else:
            res.append(i)
    for i in range(count):
        res.append(0)
    return res
