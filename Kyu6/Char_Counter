#counting characters in a string, returning them in a dict format

def count(string):
    dict = {}
    for i in string:
        if i not in dict:
            dict[i] = string.count(i)
    return dict
