#counts characters in a string that occure more than once

def duplicate_count(text):
    counter = 0
    lowtext = text.lower()
    for i in set(lowtext):
        if lowtext.count(i) > 1:
            counter += 1
    return counter
